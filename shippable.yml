build_image: magwas/wp_oauth_plugin
language: python
python:
  - 2.7
build:
  ci:
    - tools/script
  on_failure:
    - tools/postci
  on_success:
    - tools/postci
    - rsync -e "ssh -p 22022" -ar target/ shippable@demokracia.rulez.org:/var/www/adadocs/joomla_oauth_plugin/$BRANCH

